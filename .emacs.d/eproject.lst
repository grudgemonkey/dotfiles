;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list
      '(("eproject" "/home/grudg/.emacs.d/emacsforpython/extensions/eproject")))

(setq prj-last-open nil)

(setq prj-frame-pos
      '((top . 0)
	(left . 0)
	(width . 239)
	(height . 55)))

